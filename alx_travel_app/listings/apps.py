"""Listings application configuration."""

from django.apps import AppConfig


class ListingsConfig(AppConfig):
    """Listings application configuration."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "listings"
